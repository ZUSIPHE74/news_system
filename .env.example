# Database configuration
# Set USE_SQLITE to True to use SQLite (default). Set to False to connect to MariaDB/MySQL.
USE_SQLITE=True

# MariaDB/MySQL Credentials (required only when USE_SQLITE=False)
DB_NAME=news_db
DB_USER=root
DB_PASSWORD=
DB_HOST=127.0.0.1
DB_PORT=3306
